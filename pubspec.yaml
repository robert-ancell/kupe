name: kupe
description: An OpenStreetMap editor.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  osm:
    path: ../osm.dart

dev_dependencies:
  test: ^1.26.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
